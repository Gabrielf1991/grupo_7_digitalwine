<!DOCTYPE html>
<html lang="en">
   
    <%- include("../partials/head.ejs")%>
    
<body>
    
    <%- include("../partials/header.ejs")%>
        
    <main>     
        <div class="container">
            <div class="titles">
                <h1>Iniciá sesión</h1>
            </div>
                <form method="POST" action="/login" id="login-form">

                <div>
                    <label for="email"></label>
                    <input type="email" name="email" id="email" placeholder="E-mail" class="estilo-form">
                    <br>
                    <small class="smallError">
                        <%= typeof errors === 'undefined' ? '' : errors.email ? errors.email.msg : '' %>
                    </small>
                </div>
                <div>
                    <label for="password"></label>
                    <input type="password" name="password" id="password" placeholder="Contraseña" class="estilo-form">
                    <br>
                    <small class="smallError">
                        <%= typeof errors === 'undefined' ? '' : errors.password ? errors.password.msg : '' %>
                    </small>
                </div>        
                <div> 
                    <input type="checkbox" name="recordarme"> Recordarme
                </div>
                <div> 
                <p><button type="submit" value="login" class="register">Ingresá</button></p>
                </div>
             </form>

        </div>  

    </main>

    <%- include("../partials/footer.ejs")%>
   <script src="/js/scriptLogin.js"></script> 
</body>
</html>